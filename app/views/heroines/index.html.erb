<h2>All Heroines</h2>

<%= button_to "Click Here to Create New Heroine!", new_heroine_path, method: :get %>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%=link_to heroine.super_name, heroine_path(heroine.id) %>
    </li>
    <br>
  <% end %>

</ul>
